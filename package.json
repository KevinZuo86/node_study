{
  "name": "medweb",
  "version": "1.0.0",
  "description": "medweb",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp": "^3.8.8",
    "rootpath": "^0.1.2",
    "browserify": "^5.12.0"
  }
}
